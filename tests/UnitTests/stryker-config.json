{
  "stryker-config": {
    "project": "BCrypt.Net.csproj",
    "target-framework": "net10.0",
    "reporters": ["progress", "html", "json"],
    "log-level": "info",
    "concurrency": 4,
    "additional-timeout": 30000,
    "mutate": [
      "!**/obj/**",
      "!**/Properties/**"
    ]
  }
}
